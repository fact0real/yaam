# راهنمای جامع معماری و توسعه فنی نرم‌افزار YAAM (برای توسعه‌دهندگان)

> **مخاطبان سند:** توسعه‌دهندگان فعلی و آینده، معماران نرم‌افزار و مهندسان ارتباطات رادیویی.  
> **محیط توسعه:** Xcode 16+ / macOS 15.0+  
> **زبان و فریم‌ورک‌ها:** Swift 6, SwiftUI, AppKit, Combine, SQLite3, CoreAudio.  

---

## ۱. نقشه راه و فلسفه طراحی معماری

نرم‌افزار **YAAM (Yet Another ADIF Manager)** یک سیستم بومی (Native) مک برای مدیریت ایستگاه رادیوآماتوری، ثبت لاگ، مسابقات رادیویی (Contests)، مدولاسیون‌های دیجیتال (FT8 بومی) و تحلیل‌های آماری پیشرفته است.

### اصول بنیادین:
1. **اولویت ذخیره‌سازی محلی و پایداری داده (Local-First & Zero Data Loss):**
   - تمامی لاگ‌ها در پایگاه داده SQLite محلی مجهز به مکانیزم WAL (Write-Ahead Logging) ذخیره می‌شوند.
   - قبل از هر عملیات ادغام، همگام‌سازی ابری یا ایمپورت، نقطه بازگشت خودکار (Restore Point) ثبت می‌گردد.
2. **رابط کاربری با نرخ فریم ۶۰/۱۲۰ بدون افت سرعت:**
   - برای لاگ‌بوک‌های بسیار بزرگ (بیش از ۱۰۰,۰۰۰ تماس)، پردازش‌ها، شاخص‌های تاییدیه، محاسبات خورشیدی و نرمال‌سازی در صف‌های پس‌زمینه انجام شده و نتیجه به صورت اسنپ‌شات تغییرناپذیر به `@MainActor` تحویل داده می‌شود.
3. **زمان‌بندی بسیار دقیق صوتی و شبکه رادیویی:**
   - کنترلر رادیو با استفاده از پکت‌های UDP و فریم‌های صوتی LPCM16 در اینتروال‌های دقیق ۱۰ میلی‌ثانیه کار می‌کند. در حین ارسال (TX)، صف صدای ورودی (RX) موقتاً قطع می‌شود تا پایداری فرستنده دچار کوچک‌ترین پرش تایمر نگردد.

---

## ۲. دیاگرام لایه‌های سیستم

```
┌────────────────────────────────────────────────────────────────────────┐
│                        لایه‌ی رابط کاربری (SwiftUI & AppKit)             │
│   • LogTableView       • OperatorDeskView    • StatisticsView          │
│   • LocalActivityMatrix • Leaderboard360Radar • FT8StationView          │
│   • ContestWorkspace   • Azimuthal/Globe3D   • SixMeterWatchView       │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼────────────────────────────────────┐
│                    لایه‌ی مدیریت وضعیت مرکزی (AppState)                 │
│   • AppState.swift              • AppStatePersistence.swift            │
│   • AppStateConnectivity.swift  • AppStateQSLHub.swift                 │
│   • AppStateRadioContest.swift  • AppStateSyncCenter.swift             │
└───────────────┬───────────────────┬───────────────────┬────────────────┘
                │                   │                   │
┌───────────────▼────────┐ ┌────────▼────────┐ ┌────────▼────────┐
│  موتورهای محاسباتی هسته│ │ ترنسپورتهای سخت‌افزار│ │  ذخیره‌سازی و ابر   │
│ • LogSearchEngine      │ │ • IcomNetworkRadio │ │ • SQLite Database    │
│ • ConfirmationOpp...   │ │ • TCIClient (SunSDR│ │ • macOS Keychain     │
│ • DuplicateReview      │ │ • FLRigClient      │ │ • QRZ Rank API       │
│ • AstronomicalSolar    │ │ • Hamlib rigctld   │ │ • LoTW / TQSL        │
│ • CountryNameNormalizer│ │ • WinKeyer CW      │ │ • Club Log Spots     │
│ • AmateurBandPlan (11) │ │ • FT8EngineService │ │ • PSK Reporter       │
└────────────────────────┘ └────────────────────┘ └──────────────────────┘
```

---

## ۳. تفکیک پوشه‌ها و ماژول‌های کلیدی در `YAAM/`

### ۱) مدیریت وضعیت مرکزی (`AppState`)
- `AppState.swift`: هسته اصلی متغیرهای لاگ، انتخاب‌ها، اسناد جستجوی سریع.
- `AppStatePersistence.swift`: هماهنگی پایگاه داده SQLite، ذخیره خودکار و پشتیبان‌گیری.
- `AppStateQSLHub.swift`: صف مقاوم آپلود/دانلود به LoTW، QRZ و eQSL با قابلیت تلاش مجدد.
- `AppStateConnectivity.swift`: مدیریت وضعیت اتصالات شبکه و رادیوها.
- `AppStateRadioContestFeatures.swift`: کارتابل مسابقات و موتور ارسال خودکار پیام (ESM).
- `AppStateSyncCenter.swift`: تطبیق و یکپارچه‌سازی تاییدیه‌های محلی با سرورها.

### ۲) ارتباطات رادیویی شبکه و فرستنده (`IcomNetworkRadio.swift`)
- ارتباط از طریق پروتکل شبکه آیکوم روی پورت‌های UDP زیر:
  - **۵۰۰۰۱:** تبادل کنترل، احراز هویت، توکن‌ها و فرامین CI-V.
  - **۵۰۰۰۲:** استریم صوتی دوطرفه (۴۸ کیلوهرتز، ۱۶ بیتی خطی، فریم‌های ۱۰ میلی‌ثانیه‌ای).
  - **۵۰۰۰۳:** دیتای طیف و آبشار (FFT Scope Data).
- **نکته حیاتی برای رادیو IC-7300MK2:**
  - دستور `DATA MOD` در مدل جدید روی زیردستور **`1A 05 00 85`** قرار دارد (`05 = LAN`، `03 = USB`، `00 = MIC`). هرگز از کد قدیمی `00 67` استفاده نکنید زیرا رادیو روی MIC مانده و باعث فیدبک شدید RF و نوسان توان می‌گردد.
  - در شروع ارسال، ۸ فریم اول با فاصله ۱ میلی‌ثانیه‌ای پیش‌بارگذاری می‌شوند تا بافر رادیو پر شده و صدا هرگز منقطع نشود.

### ۳) ماتریس فعالیت ۲۴ ساعته و آمار (`LocalActivityMatrixView.swift` و `StatisticsView.swift`)
- تبدیل تاریخ و ساعت UTC لاگ به زمان محلی سیستم اپراتور (`TimeZone.current`).
- نمایش هیت‌مپ در ۳ حالت: روز در برابر ساعت محلی، باند در برابر ساعت محلی، و روز در برابر باند.
- استفاده از ۱۱ باند استاندارد رادیوآماتوری (`160m`, `80m`, `60m`, `40m`, `30m`, `20m`, `17m`, `15m`, `12m`, `10m`, `6m`).
- تفکیک وضعیت خورشیدی روز و شب (سحرگاه، روشنایی روز، غروب و شب) بالای ساعات ۲۴ گانه.
- پنل بازرسی هوشمند با کلیک روی هر سلول برای مشاهده کشورهای کارشده، پرچم، تعداد تاییدشده و پرش به رکورد لاگ.

### ۴) داشبورد رادار ۳۶۰ درجه (`LeaderboardView.swift`)
- پتانسیل باند-کشور: **۳,۷۴۰** ($11 \times 340$).
- کارت `DAILY PERFORMANCE`: استفاده از ساختار `DailyConfirmedPerformance` برای محاسبه میانگین تماس‌های تاییدشده روزانه، نمایش نمودار میله‌ای مینیاتوری روزهای اخیر لاگ به جای صفر مطلق در روزهای بدون فعالیت زنده.

### ۵) موتور دیجیتال FT8 بومی (`FT8EngineService.swift` و `FT8StationView.swift`)
- پردازش سیگنال صوتی با فرکانس کاری استاندارد و دیکود همزمان ایستگاه‌ها با فریم‌ورک `FT8808Engine`.
- نمایش آبشار گرافیکی، جدول ایستگاه‌های آنلاین و توالی پاسخ خودکار (Auto-Sequencer: CQ -> Reply -> Report -> 73).

### ۶) پایگاه داده و ادغام فایل‌ها (`LogbookDatabase.swift` و `DuplicateReview.swift`)
- ساختار کلید یکتای مرکب:
  $$\text{UniqueKey} = \text{CALL} + \text{DATE} + \text{TIME(4-Digit)} + \text{BAND} + \text{MODE}$$
- جلوگیری از ثبت تماس‌های تکراری و ادغام فیلدهای تاییدیه LoTW/QRZ بدون حذف یادداشت‌های کاربر.

---

## ۴. راهنمای کامپایل، تست و اعتبارسنجی

### کامپایل پروژه در ترمینال:
```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme YAAM -destination 'platform=macOS' build
```

### اجرای تست‌های رگرسیون (۲۱ مجموعه تست):
```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme YAAM -destination 'platform=macOS' test
```

### مهم‌ترین سوئیت‌های تست که پس از هر تغییر باید بررسی شوند:
1. `QSOIdentityRegression.swift`: صحت ساختار کلید یکتا و عدم تکرار رکوردها.
2. `ConfirmationSyncRegression.swift`: پردازش صحیح لاگ‌های ورودی از LoTW و QRZ.
3. `IcomNetworkTransportRegression.swift`: اعتبارسنجی ارسال بسته‌های شبکه و فرامین CI-V.
4. `CountryFlagLookupRegression.swift`: صحت نگاشت کدهای کشوری به ایموجی پرچم‌ها.

---

## ۵. نکات کلیدی برای توسعه‌دهندگان بعدی

1. **مدیریت زمان‌ها:** تمام ذخیره‌سازی‌ها و تراکنش‌های ADIF باید **۱۰۰٪ با زمان UTC** انجام شوند. زمان محلی تنها در لایه نمایش برای راحتی اپراتور به کار گرفته می‌شود.
2. **پایداری فرستنده رادیویی:** هنگام اضافه کردن امکانات به کنترلر رادیو، اطمینان حاصل کنید که هیچ پردازش سنگینی روی ترد صف شبکه UDP اجرا نشود.
3. **تعداد باندهای آماتوری:** همواره ۱۱ باند اصلی را به عنوان مبنای استانداردهای مسابقات و رادار در نظر بگیرید.
